version: 2

models:
  - name: fct_orders
    columns:
      - name: amount
        tests:
          - dbt_utils.expression_is_true:
              expression: "amount > 0"
              config:
                where: "status = 'delivered'"
