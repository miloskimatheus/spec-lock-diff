The test still runs and can no longer stop anything.
expect count 1
expect line BLOCK\tmodels/marts/fct_orders.yml\tfct_orders\ttest 'unique' on fct_orders.order_id was downgraded from error to warn, so it cannot block\t[G3]
