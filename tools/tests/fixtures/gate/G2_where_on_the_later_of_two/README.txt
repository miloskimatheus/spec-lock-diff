A where added to whichever of two same-named tests sorts later by arguments.
The gate kept one config per (model, column, test name), so this one was never
compared and the weakening was invisible.
expect count 1
