The middle commit carries a pre-registration with an open interval, which check would have
blocked; the last commit is green again. Rule 5 says the red one was never to be committed.
expect exit 0
expect rules I6
