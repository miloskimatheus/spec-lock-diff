The model's sql is rewritten and no meta.pre_registration is declared. compare
only ever looks at models that carry one, so this model would reach Stage E
with nothing to be held against.
expect rules G8
expect count 1
