version: 2

models:
  - name: fct_orders
    meta:
      spec:
        grain: "one row per order per day"
        primary_key: [order_id]
        tier: standard
        metrics: {}
        known_edges: []
        sensitive_columns: []
      pre_registration:
        type: data_change
        reason: "code follows the spec that was written first"
        row_delta: {min: 0, max: 5000}
        removed_pks: {max: 0}
        altered_columns: []
        metrics:
          {}
    columns:
      - name: order_id
