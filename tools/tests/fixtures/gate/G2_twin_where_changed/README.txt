Both twins stay; the second one's where is narrowed. A regression guard, not a
proof: the point is that holding declarations as a bag still lets G2 see a
filter change, rather than reading it as one removed and one added.
expect rules G2
expect count 1
