A model outside the marts, with a spec and a reconciliation, changed in the
same branch as its reconciliation. The exam and the answer key, wherever the
model lives: the gate reads the sql of every model under models/.
