version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id

unit_tests:
  - name: cancelled_orders_are_excluded
    model: fct_orders
    given:
      - input: ref('stg_orders')
        rows:
          - {order_id: 1, status: cancelled}
    expect:
      rows: []
