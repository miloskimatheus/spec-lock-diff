version: 2

models:
  - name: fct_refunds
    meta:
      spec:
        grain: "one row per refund"
        primary_key: [refund_id]
        tier: standard
        metrics:
          gross_revenue: "sum of order_total before discounts and taxes"
        known_edges: []
        sensitive_columns: []
      pre_registration:
        type: data_change
        reason: "refunds of the closed month, merged last quarter"
        row_delta: {min: 0, max: 12000}
        removed_pks: {max: 0}
        altered_columns: [gross_revenue]
        metrics:
          gross_revenue: {delta_pct: {min: 0.0, max: 0.8}}
    columns:
      - name: refund_id
        tests:
          - unique
      - name: gross_revenue
