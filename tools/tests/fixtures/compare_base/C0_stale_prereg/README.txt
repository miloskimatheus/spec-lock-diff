A diff for a model this branch did not touch, held against the pre-registration
main already had. A prediction written for an earlier change is not evidence
about this one, so it counts as absent.
expect count 1
