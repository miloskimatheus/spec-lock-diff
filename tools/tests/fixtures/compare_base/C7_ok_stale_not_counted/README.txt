fct_refunds carries the pre-registration of a pull request merged long ago, and
this one did not touch it. With --base, compare reads the merge-base and does
not ask for its diff. Without --base it would (see test_compare.py).
expect absent C7
