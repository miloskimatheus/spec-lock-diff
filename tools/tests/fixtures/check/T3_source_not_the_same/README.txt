Two unit tests each give a source that is almost the one the model reads: the same table in The model carries a pre-registration: it is the one being changed, and the gap blocks.
another schema, and another table in the same schema. Neither is the model's source.
expect count 2
expect absent I7
