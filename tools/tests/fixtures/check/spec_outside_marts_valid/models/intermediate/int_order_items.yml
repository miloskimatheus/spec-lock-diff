version: 2

models:
  - name: int_order_items
    meta:
      spec:
        grain: "one row per order item"
        primary_key: [order_item_id]
        tier: standard
        metrics: {}
        known_edges: []
        sensitive_columns: []
    columns:
      - name: order_item_id
        tests:
          - unique
