row_delta declares a min above its max, an interval nothing can fall inside.
expect rules P2
expect count 1
expect line BLOCK\tmodels/marts/fct_orders.yml\tfct_orders\tpre_registration.row_delta has min 100, which is above max 0\t[P2]
