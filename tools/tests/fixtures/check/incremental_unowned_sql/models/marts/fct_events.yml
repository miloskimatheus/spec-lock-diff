version: 2

models:
  - name: fct_events
    meta:
      spec:
        grain: "one row per event"
        primary_key: [event_id]
        tier: standard
        metrics: {}
        known_edges: []
        sensitive_columns: []
    columns:
      - name: event_id
        tests:
          - unique
