A unit test that names an edge the spec does not have, the edge the spec has named by nobody, and
no pre-registration: two readings, no block.
expect exit 0
expect rules I7
expect absent T2
expect count 2
