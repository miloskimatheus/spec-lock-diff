The model reads ref('orders') and the unit test gives source('raw', 'orders'): the same name is The model carries a pre-registration: it is the one being changed, and the gap blocks.
not the same input.
expect count 1
expect absent I7
