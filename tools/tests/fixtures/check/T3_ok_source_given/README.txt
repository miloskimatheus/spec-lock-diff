The model reads a source, which the unit test gives rows for; a ref built from a variable, and a
source whose schema is one, have no quoted name to read and are not seen; refs in comments are
not read. Nothing to report.
expect absent I7
