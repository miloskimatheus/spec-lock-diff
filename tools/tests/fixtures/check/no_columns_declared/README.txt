The model declares no columns at all, so the primary key cannot be verified.
Fail closed: what cannot be checked is not approved.
expect rules S3
expect line BLOCK\tmodels/marts/fct_orders.yml\tfct_orders\tcannot verify primary_key: model declares no columns\t[S3]
