version: 2

models:
  - name: fct_orders
    meta:
      spec:
        grain: "one row per order"
        primary_key: [order_id]
        tier: standard
        metrics:
          gross_revenue: "sum of order_total before discounts and taxes"
        known_edges: []
        sensitive_columns: []
      pre_registration:
        type: data_change
        reason: "a column the model does not have"
        row_delta: {min: 0, max: 10}
        removed_pks: {max: 0}
        altered_columns: [net_revenue]
        metrics:
          gross_revenue: {delta_pct: {min: 0.0, max: 0.8}}
    columns:
      - name: order_id
        tests:
          - unique
      - name: gross_revenue
