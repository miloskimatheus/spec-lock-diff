A model production does not have. There is no percentage to predict, so the
pre-registration declares the value itself, written around the anchor in
external_validation, and the diff reports the value it read.
expect exit 0
expect rules I2
expect absent C4
