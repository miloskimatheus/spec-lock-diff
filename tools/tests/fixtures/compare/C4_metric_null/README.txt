Production is 0, so the percentage cannot be computed. A number nobody
can evaluate does not pass.
expect count 7
expect line BLOCK\tdiff.json\tfct_orders\tmetric gross_revenue cannot be evaluated: the production value is 0; a model production does not have is pre-registered by value, not by delta_pct\t[C4]
