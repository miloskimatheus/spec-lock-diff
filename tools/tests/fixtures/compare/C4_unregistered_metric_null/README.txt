The diff measured a metric that was never pre-registered, and could not put a
percentage on it: a number nobody can evaluate is still not a number anybody
approved.
expect line BLOCK\tdiff.json\tfct_orders\tmetric order_count moved by a percentage that cannot be evaluated, because the production value is 0 and was not pre-registered\t[C4]
